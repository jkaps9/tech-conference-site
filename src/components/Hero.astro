---
import { Image } from "astro:assets";

import heroImage from "../assets/images/avatar-elena-vasquez.webp";
import HorizonBG from "../assets/images/bg-horizon-text.svg";
import ArrowSVG from "../assets/images/icon-arrow-right.svg";
---

<section class="hero">
  <div class="container">
    <div class="hero__intro">
      <div class="bg-image">
        <HorizonBG />
      </div>
      <h1>where code meets the machine_</h1>
      <div class="hero__intro__bottom">
        <p class="date">Nov 15-17, 2026</p>
        <p class="location">Pier 70, SF</p>
      </div>
    </div>
    <div class="hero__featured-keynote">
      <div class="featured-keynote__text">
        <p class="card-title">// Featured Keynote</p>
        <div class="speaker">
          <h2 class="speaker__name">elena vasquez</h2>
          <p>Principal frontend engineer</p>
          <p>@Bytecraft</p>
        </div>
        <div class="event">
          <div class="event__info">
            <p class="event__title">The next frontier of web development</p>
            <p class="event__date">Nov 15 / 9:00 / Room A</p>
          </div>
          <a
            href="#"
            class="btn btn--light"
          >
            View Talk
            <ArrowSVG />
          </a>
        </div>
      </div>
      <div class="featured-keynote__image">
        <Image
          src={heroImage}
          alt="Keynote speaker headshot"
        />
      </div>
    </div>
  </div>
</section>

<style lang="scss">
  .hero {
    .container {
      display: flex;
      flex-direction: column;
      gap: clamp(1.5rem, 0.9286rem + 1.1905vw, 2rem);

      @include bp(lg) {
        flex-direction: row;
      }
    }

    &__intro {
      @include flex-between;
      align-items: stretch;
      background-color: var(--color-neutral-100);
      flex-direction: column;
      min-height: clamp(13.75rem, 9.7887rem + 16.9014vw, 25rem);
      padding: clamp(1rem, 0.912rem + 0.3756vw, 1.25rem)
        clamp(1rem, 0.6479rem + 1.5023vw, 2rem);
      position: relative;

      .bg-image {
        position: absolute;
        inset: 0;

        svg {
          position: absolute;
          inset: 0;
          margin: auto;

          @include bp-max(md) {
            height: auto;
            width: 277px;
          }
        }
      }

      h1,
      p {
        color: var(--color-dark-text);
      }

      h1 {
        text-transform: lowercase;
      }

      &__bottom {
        @include flex-between;
        border-top: 2px solid var(--color-neutral-900);
        padding-top: var(--space-sm);

        p {
          @include type-body-sm;
        }
      }

      @include bp(lg) {
        min-width: 54vw;
      }
    }

    &__featured-keynote {
      --color-heading-text: var(--color-dark-text);

      background-color: var(--color-cyan-100);
      color: var(--color-dark-text);
      display: flex;

      padding: 1.25rem var(--space-sm);

      p {
        text-transform: uppercase;
      }

      .featured-keynote {
        &__text {
          align-items: stretch;
          display: flex;
          flex: 1;
          flex-direction: column;
          gap: 2rem;

          .event {
            align-items: stretch;
            display: flex;
            flex-direction: column;
            gap: var(--space-md);

            &__info {
              display: flex;
              flex-direction: column;
              gap: var(--space-xs);
            }
            &__title {
              font-weight: 700;
            }

            &__date {
              @include type-body-sm;
              color: var(--color-neutral-600);
              font-weight: 500;
            }

            @include bp(md) {
              align-items: flex-start;
              margin-top: auto;
            }
          }

          @include bp(md) {
            padding: 1.5rem;
          }
        }

        &__image {
          align-items: flex-end;
          display: flex;
          overflow: hidden;
          img {
            max-height: 408px;
            width: auto;
            transform: translateX(9vw);
          }

          @include bp-max(md) {
            display: none;
          }
        }
      }

      @include bp(md) {
        min-height: rem(400);
        padding: 0;
      }
    }

    .speaker {
      p {
        @include type-body-sm;
        color: var(--color-neutral-600);
        font-weight: 500;
      }

      &__name {
        font-size: 2rem;
        margin-bottom: var(--space-xs);
      }
    }
  }
</style>
