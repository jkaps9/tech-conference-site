---
import StarEmptySvg from "../assets/images/icon-star-empty.svg";
import StarFilledSvg from "../assets/images/icon-star-filled.svg";

interface Props {
  isFavorite?: boolean;
}

const { isFavorite = false } = Astro.props;
---

<!-- prettier-ignore -->
<button
  type="button"
  class:list={['btn--fav', { 'favorite': isFavorite }]}
>
  <div class="empty">
    <StarEmptySvg />
  </div>
  <div class="filled">
    <StarFilledSvg />
  </div>
</button>

<script>
  const favButtons = document.querySelectorAll(".btn--fav");
  favButtons.forEach((btn) => {
    // TODO: add functionality to add this to a favorites list
    btn.addEventListener("click", () => {
      if (btn.classList.contains("favorite")) {
        btn.classList.remove("favorite");
      } else {
        btn.classList.add("favorite");
      }
    });
  });
</script>

<style lang="scss">
  .btn--fav {
    background-color: var(--color-neutral-900);
    border-radius: var(--radius-pill);
    align-items: center;
    display: flex;
    padding: 0.5rem;

    .filled {
      display: none;
    }
    .empty {
      display: block;
    }

    &.favorite {
      .filled {
        display: block;
      }
      .empty {
        display: none;
      }
    }
  }
</style>
